/*!
 *
 * This library has been modified! This function has been changed: mouseProto._mouseInit
 *
 * Original Version:
 *
 *     mouseProto._mouseInit = function () {
 *   
 *       var self = this;
 *
 *       // Delegate the touch handlers to the widget's element
 *       self.element.bind({
 *         touchstart: $.proxy(self, '_touchStart'),
 *         touchmove: $.proxy(self, '_touchMove'),
 *         touchend: $.proxy(self, '_touchEnd')
 *         });
 *
 *       // Call the original $.ui.mouse init method
 *       _mouseInit.call(self);
 *     };
 *
 * 
 * New Version:
 * 
 *     mouseProto._mouseInit = function () {
 *
 *        var self = this;
 *
 *        // Delegate the touch handlers to the widget's element
 *        self.element
 *           .bind('taphold', $.proxy(self, '_touchStart'))   // IMPORTANT!MOD FOR TAPHOLD TO START SORTABLE
 *           .bind('touchmove', $.proxy(self, '_touchMove'))
 *           .bind('touchend', $.proxy(self, '_touchEnd'));
 *
 *         // Call the original $.ui.mouse init method
 *         _mouseInit.call(self);
 *     };  
 *
 * Why?
 *
 *  The original version mapped any tap start to a click. This means that you weren't able to scroll through
 *  the sortable on a mobile device, as every attempt to scroll was intercepted as a click.
 * 
 * jQuery UI Touch Punch 0.2.3
 *
 * Copyright 2011–2014, Dave Furfero
 * Dual licensed under the MIT or GPL Version 2 licenses.
 *
 * Depends:
 *  jquery.ui.widget.js
 *  jquery.ui.mouse.js
 */
(function(g){g.support.touch="ontouchend" in document;if(!g.support.touch){return}var k=g.ui.mouse.prototype,i=k._mouseInit,l=k._mouseDestroy,h;function j(c,b){if(c.originalEvent.touches.length>1){return}c.preventDefault();var a=c.originalEvent.changedTouches[0],d=document.createEvent("MouseEvents");d.initMouseEvent(b,true,true,window,1,a.screenX,a.screenY,a.clientX,a.clientY,false,false,false,false,0,null);c.target.dispatchEvent(d)}k._touchStart=function(a){var b=this;if(h||!b._mouseCapture(a.originalEvent.changedTouches[0])){return}h=true;b._touchMoved=false;j(a,"mouseover");j(a,"mousemove");j(a,"mousedown")};k._touchMove=function(a){if(!h){return}this._touchMoved=true;j(a,"mousemove")};k._touchEnd=function(a){if(!h){return}j(a,"mouseup");j(a,"mouseout");if(!this._touchMoved){j(a,"click")}h=false};k._mouseInit=function(){var a=this;a.element.bind("taphold",g.proxy(a,"_touchStart")).bind("touchmove",g.proxy(a,"_touchMove")).bind("touchend",g.proxy(a,"_touchEnd"));i.call(a)};k._mouseDestroy=function(){var a=this;a.element.unbind({touchstart:g.proxy(a,"_touchStart"),touchmove:g.proxy(a,"_touchMove"),touchend:g.proxy(a,"_touchEnd")});l.call(a)}})(jQuery);